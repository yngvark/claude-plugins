# Plugin versioning

When shipping a user-visible change to a plugin in this marketplace, bump the
`version` field in that plugin's `.claude-plugin/plugin.json` (e.g. `0.1.0` →
`0.1.1`). Consumers without marketplace auto-update enabled have no other
signal that the plugin changed.
